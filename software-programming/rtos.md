# ⏱️ Real-time Operating System (FreeRTOS)

> ESP32 และ STM32 ส่วนใหญ่รันบน **FreeRTOS** การเข้าใจเรื่อง Task และ Queue จะทำให้คุณปลดล็อกพลังที่แท้จริงของ MCU

---

## 1. Core Concepts

### 1.1 Tasks (งาน)
แทนที่จะเขียนทุกอย่างใน `loop()` เราสร้างฟังก์ชันแยกกัน เช่น `void taskSensor(void *pvParam)`
* แต่ละ Task มี **Priority** (ความสำคัญ)
    * *งานด่วน:* Safety Stop, อ่านค่ากระแสไฟ (Priority สูง)
    * *งานไม่ด่วน:* ส่ง MQTT, อัปเดตหน้าจอ (Priority ต่ำ)

### 1.2 Queues (ท่อส่งข้อมูล)
**กฎเหล็ก:** ห้ามใช้ตัวแปร Global ร่วมกันระหว่าง Task โดยไม่มีการป้องกัน (Race Condition)
* ให้ใช้ **Queue** ในการส่งข้อมูลข้าม Task แทน เพื่อความปลอดภัยของข้อมูล

### 1.3 Semaphores / Mutex (กุญแจ)
ใช้แย่งกันใช้ทรัพยากรที่มีจำกัด เช่น "การพิมพ์ออก Serial Monitor"
* ถ้า Task A พิมพ์อยู่ Task B ต้องรอจนกว่า A จะคืนกุญแจ (Give Mutex)

---

## 2. Example Code (ESP32 / Arduino Style)

```cpp
#include <Arduino.h>

// สร้าง Queue สำหรับส่งค่าอุณหภูมิ
QueueHandle_t tempQueue;

void taskReadSensor(void *parameter) {
  while(1) {
    int temp = analogRead(34);
    // ส่งข้อมูลเข้าท่อ
    xQueueSend(tempQueue, &temp, 0);
    vTaskDelay(1000 / portTICK_PERIOD_MS); // พัก 1 วิ (Non-blocking)
  }
}

void taskDisplay(void *parameter) {
  int receivedTemp;
  while(1) {
    // รอรับข้อมูลจากท่อ (ถ้าไม่มีข้อมูล ก็หลับรอ)
    if(xQueueReceive(tempQueue, &receivedTemp, portMAX_DELAY)) {
      Serial.printf("Temp: %d\n", receivedTemp);
    }
  }
}

void setup() {
  tempQueue = xQueueCreate(10, sizeof(int));
  xTaskCreate(taskReadSensor, "Sensor", 2048, NULL, 1, NULL);
  xTaskCreate(taskDisplay, "Display", 2048, NULL, 1, NULL);
}

void loop() {} // ไม่ต้องทำอะไรในนี้

```

---

[🔙 กลับสู่หน้า Software](./README.md)
